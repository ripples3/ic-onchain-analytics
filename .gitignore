# macOS
.DS_Store

# Environment secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Data files - intermediate outputs (don't commit)
data/pipeline/
data/archive/
data/*.db

# Data files - raw inputs (large, don't commit)
data/raw/

# Data files - results (commit selectively)
data/results/*.json

# Keep results CSV for reference
!data/results/*.csv

# Root level data files (shouldn't exist after cleanup)
/*.csv
/*.txt

# But allow reference files
!references/*.csv
!references/*.md

# Jupyter notebooks output
*.ipynb_checkpoints/

# Cache and checkpoint files
arkham_cache.json
enrichment_checkpoint.json

# Data directory (generated by knowledge graph)
data/
